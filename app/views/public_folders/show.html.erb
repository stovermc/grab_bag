<h1><%= @folder.name %></h1>
<%= render_breadcrumbs :separator => ' / ' %>
<div class="folder_contents">
  <table class="table">
    <div class="folders">
      <th width="75px"></th>
      <th>Name</th>
      <th>Owner</th>
      <th>Date Changed</th>
      <th>Status</th>
      <th></th>
      <% @child_folders.each do |folder|%>
        <tr>
          <!-- <td><%= image_tag(folder.owner.avatar_url) %></td> -->
          <td><%= image_tag("good_blue_folder.png", height: 40) %></td>
          <td> <%= link_to folder.name, public_folder_path(folder) %>
          <td> <%=folder.owner.username%></td>
          <td> <%=folder.updated_at%></td>
          <td> <%=folder.permission%></td>
          <td> <%= current_admin_folder_delete_button(folder) %></td>
        </tr>
      <% end %>
      <% @binaries.each do |binary|%>
        <tr>
          <!-- <td><%= image_tag("https://fakeimg.pl/300/") %></td> -->
          <td><%= image_tag("blue_file.png", height: 50) %></td>
            <td> <%= link_to binary.name, public_folders_binary_path(binary) %>
          <td> <%=binary.folder.owner.username%></td>
          <td> <%=binary.updated_at%></td>
          <td> <%=binary.status%></td>
          <td> <%= current_admin_binary_delete_button(binary) %></td>
        </tr>
      <% end %>



      <br>
    </div>
  </table>
</div>
